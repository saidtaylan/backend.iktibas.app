# HTTP -> HTTPS yönlendirme
server {
    if ($host = api.iktibas.app) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    listen 80;
    listen [::]:80;
    server_name api.iktibas.app;

    # Certbot için
    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    location / {
        return 301 https://$server_name$request_uri;
    }


}
# The ssl options will load with Certbot in the boot script
# HTTPS
server {
    listen 443 http2;
    listen [::]:443 http2;
    server_name api.iktibas.app;

    add_header Strict-Transport-Security "max-age=31536000" always;

    # Proxy ayarları
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    
    # WebSocket için
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    
    proxy_buffering off;
    proxy_read_timeout 60s;

    # Health check
    location = /health {
        access_log off;
        proxy_pass http://localhost:8000/health;
    }

    # Realtime (WebSocket)
    location /realtime/ {
        proxy_pass http://localhost:8000/realtime/;
        proxy_read_timeout 86400;  # 24 saat WebSocket için
    }

    # Storage (büyük dosyalar için)
    location /storage/ {
        proxy_pass http://localhost:8000/storage/;
        client_max_body_size 100M;
    }

    # Diğer tüm endpoint'ler
    location / {
        proxy_pass http://localhost:8000;
    }

}
